# Milestone 4 — Bot Preview & Developer-Portal Completion

You are entering plan mode to plan and then build milestone 4 of WaGuard v3.

## Context

- Read `@_build_plan/v3/prd.md` for the full project context, scope, and the cross-cutting API-first requirement.
- Read the prior v3 milestone logs (`@_build_plan/v3/milestones/1-*/`, `2-*/`, `3-*/milestone-log.md`) to inventory the per-feature APIs that already shipped with Milestones 1–3.
- Important framing: the bot/rule/KB/AI-config CRUD APIs were already delivered in earlier milestones per the API-first requirement. This milestone adds ONLY the preview/trigger-reply endpoint plus documentation completeness — do not rebuild existing CRUD.

## Your task

1. Plan the implementation for **only** milestone 4 as defined in the PRD. Do not plan or build anything from later milestones.
2. After the user confirms the plan, build only what is in milestone 4's scope: the preview/trigger-reply endpoint, the consolidated "Build a bot" getting-started guide in `/developers`, and an audit ensuring every v3 endpoint is documented to the existing v1/v2 quality bar.
3. Verify your work against the "Done when" criteria for milestone 4 in the PRD.
4. When complete, write a `milestone-log.md` in this folder (`_build_plan/v3/milestones/4-bot-api-docs/milestone-log.md`), structured as:
   - **Start with a `## What's new in the app` section at the very top** — user-facing capabilities added, short and scannable.
   - Then: what was built (files/routes/docs), decisions not pre-specified in the PRD, anything the next milestone needs, and any deviations from the PRD and why.

Ask me any clarifying questions using the AskUserQuestion tool to lock in the implementation plan for this milestone.
